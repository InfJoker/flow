name: Dev Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            label: macos-arm64
          - os: macos-latest
            target: x86_64-apple-darwin
            label: macos-x64
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            label: linux-x64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Build channel server
        run: cd channel-server && npm ci && npm run build

      - name: Install app deps
        run: cd app && npm ci

      - name: Import Apple certificate
        if: runner.os == 'macOS'
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          set -euo pipefail

          CERTIFICATE_PATH="$RUNNER_TEMP/certificate.p12"
          KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"
          KEYCHAIN_PASSWORD=$(openssl rand -base64 32)

          echo -n "$APPLE_CERTIFICATE" | base64 --decode -o "$CERTIFICATE_PATH"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          security import "$CERTIFICATE_PATH" -P "$APPLE_CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/security \
            -t cert -f pkcs12 -k "$KEYCHAIN_PATH"
          rm -f "$CERTIFICATE_PATH"
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security list-keychain -d user -s "$KEYCHAIN_PATH"

          # Extract signing identity
          IDENTITY=$(security find-identity -v -p codesigning "$KEYCHAIN_PATH" \
            | grep "Developer ID Application\|Apple Development\|Apple Distribution" \
            | head -1 | sed 's/.*"\(.*\)"/\1/')
          if [ -z "$IDENTITY" ]; then
            echo "::error::No signing identity found in keychain"
            exit 1
          fi
          echo "APPLE_SIGNING_IDENTITY=$IDENTITY" >> "$GITHUB_ENV"

      # Check whether the updater signing key secret is available. When it
      # isn't, we still build the app — we just skip producing updater
      # artifacts so main is never stuck on a missing secret.
      - name: Check updater signing availability
        id: signing
        env:
          KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: |
          if [ -n "$KEY" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::warning::TAURI_SIGNING_PRIVATE_KEY not set — updater artifacts will be skipped for this build"
          fi

      - name: Build Tauri app
        run: |
          cd app
          # Override createUpdaterArtifacts based on whether we have a signing
          # key. Tauri fails the build if the flag is true and the key is
          # missing, so we must keep them in sync.
          CONFIG_JSON='{"bundle":{"createUpdaterArtifacts":${{ steps.signing.outputs.available }}}}'
          npx tauri build --target ${{ matrix.target }} --config "$CONFIG_JSON"
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ env.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Cleanup keychain
        if: always() && runner.os == 'macOS'
        run: |
          security delete-keychain "$RUNNER_TEMP/app-signing.keychain-db" 2>/dev/null || true
          rm -f "$RUNNER_TEMP/certificate.p12"

      - name: Collect artifacts (macOS)
        if: runner.os == 'macOS'
        env:
          LABEL: ${{ matrix.label }}
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          mkdir -p artifacts
          # DMG — keep Tauri's default name (already unique per arch)
          cp "app/src-tauri/target/${TARGET}/release/bundle/dmg/"*.dmg artifacts/
          # Updater bundle — Tauri names it "<ProductName>.app.tar.gz" with no
          # arch suffix, so arm64 and x64 would collide when flattened in the
          # release job. Rename to include the matrix label for disambiguation.
          BUNDLE=$(ls "app/src-tauri/target/${TARGET}/release/bundle/macos/"*.app.tar.gz 2>/dev/null | head -1 || true)
          if [ -n "$BUNDLE" ]; then
            cp "$BUNDLE" "artifacts/agent-flow-${LABEL}.app.tar.gz"
            cp "${BUNDLE}.sig" "artifacts/agent-flow-${LABEL}.app.tar.gz.sig"
          fi

      - name: Collect artifacts (Linux)
        if: runner.os == 'Linux'
        env:
          LABEL: ${{ matrix.label }}
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          mkdir -p artifacts
          cp "app/src-tauri/target/${TARGET}/release/bundle/deb/"*.deb artifacts/
          # AppImage — rename for consistency with the macOS bundle naming so
          # the manifest generator can match on a stable label-based pattern.
          APPIMAGE=$(ls "app/src-tauri/target/${TARGET}/release/bundle/appimage/"*.AppImage 2>/dev/null | head -1 || true)
          if [ -n "$APPIMAGE" ]; then
            cp "$APPIMAGE" "artifacts/agent-flow-${LABEL}.AppImage"
            if [ -f "${APPIMAGE}.sig" ]; then
              cp "${APPIMAGE}.sig" "artifacts/agent-flow-${LABEL}.AppImage.sig"
            fi
          fi

      - uses: actions/upload-artifact@v6
        with:
          name: agent-flow-${{ matrix.label }}
          path: artifacts/*

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v6
        with:
          path: all-artifacts

      - name: Flatten artifacts
        run: |
          mkdir release
          find all-artifacts -type f \
            \( -name "*.dmg" -o -name "*.deb" -o -name "*.AppImage" \
               -o -name "*.app.tar.gz" -o -name "*.app.tar.gz.sig" \
               -o -name "*.AppImage.sig" \) \
            -exec cp {} release/ \;
          ls -la release/

      - name: Get short hash
        id: hash
        run: echo "short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      # Only generate the updater manifest when the signing secret was
      # available during build — otherwise there are no .sig files to embed.
      - name: Check updater signing availability
        id: signing
        env:
          KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: |
          if [ -n "$KEY" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Skipping latest.json — no signing key configured"
          fi

      - name: Generate latest.json
        if: steps.signing.outputs.available == 'true'
        env:
          SHORT: ${{ steps.hash.outputs.short }}
          RUN_NUMBER: ${{ github.run_number }}
          BASE: "https://github.com/InfJoker/flow/releases/download/dev"
        run: |
          set -euo pipefail

          VERSION=$(node -p "require('./app/src-tauri/tauri.conf.json').version")
          PUB_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          # Locate platform-specific updater bundles by the stable names
          # assigned during "Collect artifacts" (label-based to avoid
          # cross-arch filename collisions on macOS).
          DARWIN_ARM_BUNDLE="agent-flow-macos-arm64.app.tar.gz"
          DARWIN_X64_BUNDLE="agent-flow-macos-x64.app.tar.gz"
          LINUX_X64_BUNDLE="agent-flow-linux-x64.AppImage"
          [ -f "release/${DARWIN_ARM_BUNDLE}" ] || DARWIN_ARM_BUNDLE=""
          [ -f "release/${DARWIN_X64_BUNDLE}" ] || DARWIN_X64_BUNDLE=""
          [ -f "release/${LINUX_X64_BUNDLE}" ] || LINUX_X64_BUNDLE=""

          # Fail loud if any expected platform bundle or signature is missing.
          # A silent drop here would produce a valid-looking manifest that
          # silently stops serving updates to one arch.
          missing=0
          for pair in "darwin-aarch64:$DARWIN_ARM_BUNDLE" \
                      "darwin-x86_64:$DARWIN_X64_BUNDLE" \
                      "linux-x86_64:$LINUX_X64_BUNDLE"; do
            key="${pair%%:*}"
            file="${pair#*:}"
            if [ -z "$file" ]; then
              echo "::error::missing updater bundle for $key"
              missing=1
              continue
            fi
            if [ ! -s "release/${file}.sig" ]; then
              echo "::error::missing or empty signature for $key ($file.sig)"
              missing=1
            fi
          done
          if [ "$missing" -ne 0 ]; then
            exit 1
          fi

          DARWIN_ARM_SIG=$(cat "release/${DARWIN_ARM_BUNDLE}.sig")
          DARWIN_X64_SIG=$(cat "release/${DARWIN_X64_BUNDLE}.sig")
          LINUX_X64_SIG=$(cat "release/${LINUX_X64_BUNDLE}.sig")

          # Use the monotonic GitHub Actions run number as a numeric pre-release
          # identifier so semver ordering is strictly monotonic across builds.
          # Git short hashes compared lexicographically would yield non-monotonic
          # ordering and break rolling-dev updates on some machines.
          FULL_VERSION="${VERSION}-dev.${RUN_NUMBER}"

          export FULL_VERSION PUB_DATE SHORT RUN_NUMBER
          export DARWIN_ARM_BUNDLE DARWIN_X64_BUNDLE LINUX_X64_BUNDLE
          export DARWIN_ARM_SIG DARWIN_X64_SIG LINUX_X64_SIG

          node - <<'NODE' > release/latest.json
          const version = process.env.FULL_VERSION;
          const pubDate = process.env.PUB_DATE;
          const base = process.env.BASE;
          const platforms = {};
          function add(key, file, sig) {
            if (!file || !sig.trim()) {
              throw new Error(`missing bundle or signature for ${key}`);
            }
            platforms[key] = {
              signature: sig.trim(),
              url: `${base}/${file}`,
            };
          }
          add('darwin-aarch64', process.env.DARWIN_ARM_BUNDLE, process.env.DARWIN_ARM_SIG);
          add('darwin-x86_64', process.env.DARWIN_X64_BUNDLE, process.env.DARWIN_X64_SIG);
          add('linux-x86_64', process.env.LINUX_X64_BUNDLE, process.env.LINUX_X64_SIG);
          if (Object.keys(platforms).length === 0) {
            throw new Error('no platforms resolved — refusing to publish empty manifest');
          }
          const manifest = {
            version,
            notes: `Dev build ${process.env.RUN_NUMBER} (${process.env.SHORT}) from main`,
            pub_date: pubDate,
            platforms,
          };
          process.stdout.write(JSON.stringify(manifest, null, 2) + '\n');
          NODE
          cat release/latest.json

      - name: Delete previous dev release
        run: gh release delete dev --yes 2>/dev/null || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete previous dev tag
        run: git push origin :refs/tags/dev 2>/dev/null || true

      - name: Create dev release
        run: |
          gh release create dev release/* \
            --title "dev-${{ steps.hash.outputs.short }}" \
            --notes "Development build from \`${{ steps.hash.outputs.short }}\` on main.

          **Install:**
          - macOS (Apple Silicon): Download the arm64 \`.dmg\`, open, drag to Applications
          - macOS (Intel): Download the x64 \`.dmg\`
          - Linux: Download \`.AppImage\` (chmod +x, run) or \`.deb\` (sudo dpkg -i)

          Also build the channel server: \`cd channel-server && npm install && npm run build\`" \
            --prerelease
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
